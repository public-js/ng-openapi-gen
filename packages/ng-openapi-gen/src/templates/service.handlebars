/* tslint:disable */
/* eslint-disable */
{{autoGenerationNotice}}
import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Inject, Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';

import { {{rootUrlToken}} } from '../{{configurationFile}}';
import { {{requestBuilderClass}}, {{responseClass}} } from '../{{requestBuilderFile}}';

{{#imports}}import { {{{typeName}}}{{#useAlias}} as {{{qualifiedName}}}{{/useAlias}} } from '{{{file}}}';
{{/imports}}

{{{tsComments}}}@Injectable()
export class {{typeName}} {
  constructor(
    @Inject({{rootUrlToken}}) private rootUrl: string,
    @Inject(HttpClient) private http: HttpClient,
  ) {}

{{#operations}}
{{>operationPath}}
{{#variants}}{{>operationResponse}}{{>operationBody}}{{/variants}}
{{/operations}}
}
